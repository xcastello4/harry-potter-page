---
const { name, house, image } = Astro.props;
---
<li>
    <div class="flex items-center gap-x-6">
      <img class="h-16 w-16 rounded-full" src={image} alt={name}>
      <div>
        <h3 class="text-base font-semibold leading-7 tracking-tight text-gray-900 dark:text-white">{name}</h3>
        <p class="text-sm font-semibold leading-6 text-yellow-600">{house}</p>
      </div>
    </div>
</li>